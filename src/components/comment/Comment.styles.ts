import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  comment: {
    paddingHorizontal: 24,
    marginBottom: 24,
  },
  nestedComment: {
    marginTop: 16,
    marginBottom: 0,
    paddingRight: 0,
  },
});
