import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  filtersWrapper: {
    marginVertical: 16,
    marginHorizontal: 24,
  },
  filters: {
    flexDirection: 'row',
  },
});
